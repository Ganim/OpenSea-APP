/**
 * Items Module Configuration
 * Configuração do módulo de items
 */

import { itemsConfig as baseConfig } from '@/config/entities/items.config';

/**
 * Exporta a configuração base do módulo de items
 */
export const itemsConfig = baseConfig;
